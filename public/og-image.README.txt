PLACEHOLDER NOTE - og-image.jpg

The site head (src/layouts/Base.astro) references /og-image.jpg for Open Graph
and Twitter card previews. Drop a real 1200 x 630 JPG named "og-image.jpg" in
this public/ folder.

Suggested: the Bee Magical wordmark and bee over the dark starfield, with one or
two of the brightest paintings, so link previews read as the gallery at a glance.

Until that file exists, social previews will 404 the image but nothing else breaks.
